const1: |
  'Common constant string. Notice, that it is in single quotes, so...'

const2: |
  '... it will be inserted with them, like a string, not SQL expression'

some_common_query: |
  SELECT * FROM projects